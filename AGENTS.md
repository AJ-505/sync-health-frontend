# AGENTS.md

Sync Health Frontend is the UI for the corporate chronic disease risk prediction platform.

## Essentials (Always Apply)

- Package manager: use `pnpm` only.
- Do not assume scripts: verify available commands in `package.json`.
- Standard validation before handoff: `pnpm build` and `pnpm lint`.
- Product requirements source of truth: `docs/PRD.md`.

## Progressive Disclosure

- If a task touches scope, priorities, or feature behavior, read `docs/PRD.md` first.
